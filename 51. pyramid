#include <iostream>
using namespace std;
class LPT
{
	public:
		int i,j,n;
		void p(int n)
		{
			for(i=0;i<n;i++){
		for(j=1;j<=n-i;j++){
			cout<<" ";
		}
		for(j=1;j<=i;j++){
			cout<<" * ";
		}
		cout<<endl;
	}
}
};
int main()
{
	int k;
	cin>>k;
	LPT r;
	r.p(k);
}
