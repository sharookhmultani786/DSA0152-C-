#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cout << "Enter three integers: ";
    cin >> a >> b >> c;

    int maxNum = (a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c);
    cout << "Largest number is " << maxNum << endl;

    return 0;
}
